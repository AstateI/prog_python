# ouvrir une fenetre
# menu du jeu (start option quitter)
# start = générer tous les objet du "jeu" (interfaceet tout ce qui fais le jeu)
# pendant le jeu (temps, score, règle du jeu)
# fin du jeu (affichage scor, bouton recommencer, bouton quitter)

n = int () #varriable def f(x = int ()): fonction f(x)
nombre = int () #varriable
n = int () #varriable

def OpenWindows() #fonction
